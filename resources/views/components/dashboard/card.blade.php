@props(['title' => '', 'value' => 0])
<div class="bg-white shadow-lg rounded-2xl p-5 flex flex-col items-center transition-transform duration-200 hover:scale-105 hover:shadow-2xl border-2 gradient-border">
    <div class="text-sm text-gray-500 mb-1">{{ $title }}</div>
    <div class="text-3xl font-bold text-gray-900">{{ $value }}</div>
</div>






